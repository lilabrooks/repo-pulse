# Log

## 2026-07-19

- Kit upgrade 0.1.1 → 0.3.6 via the safe updater — the first pre-manifest long-drift upgrade in the kit's dogfood fleet, and the run that exposed a kit classifier bug. A first attempt (kit 0.3.5) misclassified this repo's `CLAUDE.md` playbook as an import shim through a pipefail/SIGPIPE race in the updater's `is_import_shim` and staged the wrong candidates; the attempt was fully reset, the race fixed upstream as part of kit 0.3.6, and this upgrade re-run on the fixed updater classified correctly. What landed: `.codex/hooks` declared in a new top-level `mirrors:` list in `docs/okf-map.yml` (kit ADR 0021) so the updater now syncs the Codex hook mirrors; both `.claude` and `.codex` hook copies plus `scripts/okf` adopted from kit candidates after verifying our 2026-07-11 local hook hardening was fully absorbed upstream (kit 0.1.2's shipped-hook union — no local-only content lost) and mirrors confirmed byte-identical; the six `okf-*` skills installed (new since 0.1.3–0.3.5); `docs/index.md` restamped in place; and the provenance manifest seeded so future upgrades refresh unedited kit files in place. `CLAUDE.md` rebuilt from the kit 0.3.6 template with this repo's master objective and verification commands carried over; `AGENTS.md` rebuilt from it through the established Codex transform, now with explicit session-start reads replacing the `@`-import claim (Codex resolves no imports — the old wording overpromised) and the second-agent paragraph reframed as this port itself. New `tests/test_agent_parity.py` guards hook-mirror byte-parity and `hooks.json` wiring, skipped when the Codex stack is absent (kit ADR 0021 belt-and-suspenders). The `docs/okf-map.2.yml` starter candidate was dropped (our map carries real mappings plus the declaration). Verified: `make test` green (21 tests, including the two new parity guards), `bash scripts/okf check-stale` current, kit `verify-install` passed with zero warnings, and the SessionStart hook reports only the genuine ADR 0003 review inbox. ADR 0003 remains proposed, awaiting the owner's `docs/GOAL.md` scope decision.

## 2026-07-11

- Hardened both docs-sync Stop hooks (`.claude/hooks/check-docs-sync.sh` and the `.codex` copy) after a hook review found two pre-existing robustness gaps, inherited by the Codex port: (1) the hooks now parse `stop_hook_active` from the hook stdin payload — after a prior block in the same stop cycle they warn on stderr and allow the stop instead of blocking again, so a turn that cannot write to `docs/` (e.g. a read-only sandbox) can never loop; manual runs without a stdin payload behave exactly as before. (2) File entries in the exclusion regex are now end-anchored, so e.g. `LICENSE-MIT` or `CLAUDE.md.bak` count as code instead of silently skipping the docs check; directory prefixes (`docs/`, `.claude/`, `.codex/`) keep prefix semantics. Verified with a nine-case matrix in a scratch clone covering block, warn-and-yield, no-stdin back-compat, anchoring, and the stale-mapping path on both copies. The review also confirmed no cross-agent conflicts (each agent loads only its own hook config) and that Codex's hook output protocol matches the JSON these scripts emit. Guardrail files touched again — flagged for owner review; worth upstreaming to claude-okf-repo-kit so the next kit update does not revert it. No app code change.
- Accepted ADRs 0001 (Python web stack) and 0002 (in-memory TTL cache) at the owner's direction after verifying both are fully implemented: the pinned `requirements.txt` matches 0001's versions exactly and the suite passes on that stack; `app/cache.py` implements 0002's 300-second TTL cache with tests, `GITHUB_TOKEN` is read from the environment, `.env.example` is committed, and `.env` is git-ignored. ADR 0003 (homepage-to-repo resolution) stays proposed: it is explicitly review-only, not implemented, and acceptance requires a `docs/GOAL.md` constraint amendment reserved for the owner. Refreshed the Current state line in CLAUDE.md and AGENTS.md to say one proposed ADR remains. Docs-only change.
- Finished and committed the Codex agent-config port left in flight by the last Codex session: `AGENTS.md` (the CLAUDE.md playbook ported for Codex, which resolves the same `@docs` imports) and `.codex/` (hooks.json plus the two guardrail hook scripts). Fixed the port's broken find-and-replace references (`.Codex/` casing, a nonexistent `.codex/settings.json`, `Codex.local.md`, `Codex.2.md`), replaced the machine-specific absolute hook paths in `.codex/hooks.json` with git-root resolution per the Codex hooks docs, adapted the hook script copies to Codex (git-root `ROOT` instead of `CLAUDE_PROJECT_DIR`, AGENTS.md wording in hook messages), and corrected the `.env` guardrail claim: the mechanical read-denial exists only in Claude Code's settings, so AGENTS.md instructs Codex by policy instead. Both docs-sync hook copies now treat both agents' config (`.claude/`, `.codex/`, `CLAUDE.md`, `AGENTS.md`) as agent config rather than code — this touched the committed Claude-side guardrail `.claude/hooks/check-docs-sync.sh` (exclusion-list addition only; real source files still trigger it), flagged here for owner review. No app code, API, or frontend change.
- Updated the README badges to mirror the Skywatch two-row style, added the Claude Code provenance badge, and added an Origins note that Codex assisted with licensing, dependency pinning, Dependabot, CI, badges, and repository metadata.
- Accepted ADR 0004 (GitHub Actions CI). The workflow, README badge, `.github` OKF mappings, and Dependabot coverage for GitHub Actions were already implemented, so no extra CI changes were needed.
- Added a GitHub Actions CI workflow for pushes to `main`, pull requests, and manual runs. It runs `make test`, Python compile checks, and the OKF stale-map check on Python 3.12. Added the README CI badge, Dependabot coverage for GitHub Actions versions, and proposed ADR 0004 to record the CI decision.
- Added README badges for the MIT license, Python requirement, FastAPI stack, OKF documentation, and included specs/ADRs. The CI badge was added once a real workflow existed.
- Pinned the existing Python dependency manifest to the versions already installed in the project virtual environment (`fastapi==0.139.0`, `uvicorn==0.51.0`, `httpx==0.28.1`, `pytest==9.1.1`) so installs are repeatable and Dependabot can propose explicit version bump pull requests. Updated ADR 0001 to record the pinned-manifest policy.
- Added `.github/dependabot.yml` to check the root Python dependency manifest weekly and group available updates into one pull request. No runtime dependency, app behavior, or public API contract changed.
- Added the MIT license for the repository (`LICENSE`) with 2026 Lila Brooks copyright, and linked it from the README. No app behavior or public API contract changed.

## 2026-07-09

- Kit upgrade to claude-okf-repo-kit 0.1.1 (config and docs only, no app code). Live immediately: `.claude/settings.json` merged with `permissions.deny` rules (`Read(./.env)`, `Read(./**/.env)`) so secrets in local env files stay out of Claude Code's context mechanically; refreshed `scripts/okf` (new `new-adr`/`new-spec` scaffolds, `pending` review inbox, non-blocking unmapped-file note in `check-stale`) and SessionStart hook (now also reports kit-version drift); `.DS_Store` appended to `.gitignore`. Merged from the updater's review candidates: `docs/index.md` gained the `kit_version: 0.1.1` stamp (activates drift reporting) plus Log and Source-map links; `CLAUDE.md` ported to the 0.1.1 template — filled objective, constraints, and verification commands preserved, Current state refreshed to goal-met — gaining the example-interactions interview question, the pre-goal-met acceptance pass, the post-goal proposal protocol and ADR review mechanics, the kit-version policy, and the `.env.example`/read-denial guardrails. Remaining starter candidates deleted (repo files win); backups under `.okf-kit-backups/`. Verified: `make test` passes offline and `verify-install` passes with the stamp present. Three proposed ADRs (0001-0003) still await owner review via `bash scripts/okf pending`.
- Proposed scope change + README provenance (docs only, no code): recorded proposed ADR 0003 (homepage-to-repo resolution) for review — mapping non-GitHub URLs like `lenshq.io/products/lens-k8s-ide` to a repo, recommending the GitHub Search API behind user confirmation, and flagging that it needs a `docs/GOAL.md` constraint change (owner-reserved); not implemented. Added it to `docs/adr/index.md`. README gained an "Origins" section noting the repo was scaffolded from the `claude-okf-repo-kit` (https://github.com/lilabrooks/claude-okf-repo-kit) and documenting the widened URL input parsing, and the quickstart now lists the accepted input formats.
- Frontend input parsing (post-milestone usability fix): the form was rejecting anything but bare `owner/repo` — the HTML `pattern` attribute (one slash) plus a strict two-segment `split("/")` blocked pasted GitHub URLs before any fetch. Reported case: pasting `lenshq.io/products/lens-k8s-ide`. Added a `parseRepo()` helper (`static/app.js`) that accepts `owner/repo`, `github.com/owner/repo`, and full `https://github.com/owner/repo[/...]` URLs (stripping scheme, `www.`, host, `.git`, and any trailing path/query/fragment), removed the stale `pattern` attribute, and updated the placeholder/aria-label. Non-GitHub hosts are rejected with a clear message rather than silently mis-parsed, since the goal fixes external data to the GitHub REST API only — so `lenshq.io/...` (a product homepage, not a GitHub URL, no REST-derivable owner/repo) gets a helpful error, not a confusing 404. `docs/specs/frontend.md` updated to describe the accepted forms. No backend/API change. Verified with a Node DOM-stub harness driving the real submit handler (7 wiring cases: URL/host/.git/bare/space-encoding resolve to the right encoded fetch; non-GitHub and single-segment inputs fetch nothing and show an error; 404 detail shown verbatim) plus a 18-case pure-logic table, and live against the running server (served `app.js` carries `parseRepo`, `/` has no `pattern`, `github.com`-style query resolves through to the GitHub API). JS still has no automated harness in `make test` per the no-Node constraint; these checks were throwaway scripts.

- Milestone 1 (skeleton): FastAPI app with `/api/health` and the static index shell, pytest harness, and Makefile (`make test`, `make run`). Stack decision recorded as proposed ADR 0001 (FastAPI/uvicorn/httpx/pytest) per the decision policy — awaiting owner review. API contract started in `docs/specs/api.md` (health + index routes). Project ignores added for `.venv/`, `__pycache__/`, bytecode, and pytest cache.
- Known deprecation: starlette TestClient warns that `httpx` support is deprecated in favor of `httpx2`; tests pass, revisit with ADR 0001 if it breaks.
- Milestone 2 (GitHub summary API): `GET /api/repo/{owner}/{repo}` normalizes repo facts, open issues vs PRs (Link-header count), latest release, 30-day commit activity, and the pulse verdict; 404 and rate-limit errors mapped per spec. `docs/specs/api.md` carries the full contract and testability note; suite runs offline via httpx.MockTransport.
- Milestone 3 (frontend dashboard): form plus pulse banner and stat cards rendering the summary, loading/error states, dark-mode aware; served from `/` with assets under `/static/`. New `docs/specs/frontend.md` contract; `static/**` mapped to it. JS behavior has no automated harness (no Node, per constraints) — manual browser check is the documented verification.
- Milestone 4 (rate-limit friendliness): 300s in-memory TTL cache on repo summaries (proposed ADR 0002) and optional `GITHUB_TOKEN` from the environment; `.env.example` committed, `.env`/`.env.*` git-ignored with `!.env.example` kept trackable, ignore rules added before any env file existed per the security guardrail. Errors are never cached. API spec gained the caching/rate-limit section; `app/**` mapping now includes ADR 0002.
- Milestone 5 (polish): README quickstart added and reproduced on a clean checkout — fresh venv, `make test` (19 passed), live `make run` with `/api/health`, `/`, and static assets 200, plus a live anonymous query of `psf/requests` returning the full summary and an unknown repo returning 404. Verdict edge cases covered end-to-end with mocks: empty repo (commits 409, `pushed_at` null) -> dormant with "no recorded pushes", archived overriding recent activity, and a `quiet` unit test (no-release -> null was already covered). Two gaps found and fixed during verification: (1) the commits call bypassed the shared status handling, so a rate-limited commits fetch returned 500 instead of the spec's 503 — status checks unified in `app/github.py`; (2) an invalid `GITHUB_TOKEN` surfaced as an unhandled 500 — now mapped to 502 with guidance (new error case in `docs/specs/api.md`, verified live with a dummy token). `make run` now sources `.env` when present, making the copy-and-fill flow in `.env.example` real (ADR 0002 updated; Makefile mapping extended to ADR 0002). All milestones are checked and every success criterion in `docs/GOAL.md` passes, including `git check-ignore .env`.
