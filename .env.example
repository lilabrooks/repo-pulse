# Optional: raises the GitHub API rate limit from 60 to 5,000 requests/hour.
# Copy to .env (git-ignored; `make run` loads it) and fill in, or export in
# your shell. Never commit a real token.
GITHUB_TOKEN=
